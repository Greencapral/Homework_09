[project]
name = "homework-07"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.2",
    "python-dotenv>=1.2.1",
    "faker>=40.8.0",
]

[dependency-groups]
dev = [
    "black>=26.1.0",
]

[tool.black]
line-length = 59
